import 'package:flutter/material.dart';

class PhotoInfoScreen extends StatelessWidget {
  const PhotoInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
